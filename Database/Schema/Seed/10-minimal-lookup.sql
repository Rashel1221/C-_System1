-- ============================================================================
-- 10-minimal-lookup.sql (Optional)
-- Intentionally empty. Add only essential lookup rows if the app
-- cannot start or certain forms fail without them.
-- ============================================================================
-- Example (uncomment & adjust if needed):
-- INSERT INTO dbo.Users (UserName, [Password], User_Type) VALUES ('admin','CHANGE_ME','Admin');
-- GO

PRINT '10-minimal-lookup.sql (no seed data added).';
GO
